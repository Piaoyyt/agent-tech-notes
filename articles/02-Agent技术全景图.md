# Agent 技术全景图：从概念到落地，一张图看懂 Agent 技术栈

> 这是「Agent技术笔记」的第二篇文章，也是整个知识体系的"地图"。建议收藏，后续每篇文章都会标注在这张图中的位置。

## 先说结论

Agent 技术不是单一的技术点，而是一个**多层架构的工程体系**。从底层的 LLM 能力，到中间层的工具调用、记忆管理、决策推理，再到上层的多 Agent 协作与业务编排——每一层都有自己的技术挑战和最佳实践。

这篇文章不会深入任何一个技术细节（后续会逐一展开），而是帮你建立**全局视野**：Agent 技术的全景长什么样，各部分之间怎么关联，你应该从哪里开始学习。

## Agent 的本质：LLM + 工具 + 自主决策

在进入全景图之前，先统一一个认知：

**Agent ≠ 聊天机器人。**

聊天机器人是"你问，它答"——用户驱动，单轮交互。Agent 是"你给一个目标，它自己想办法完成"——目标驱动，多轮自主决策。

这个区别的核心在于三个关键词：

1. **感知（Perception）**：Agent 能理解任务目标和环境上下文
2. **推理（Reasoning）**：Agent 能自主规划步骤、做出决策
3. **行动（Action）**：Agent 能调用工具、执行操作、产生实际效果

这三层能力叠加在一起，就构成了 Agent 最基本的运作模型：**感知 → 推理 → 行动 → 观察 → 再推理 → 再行动...**，形成一个闭环。

其中，**观察（Observation）**是连接行动与下一轮推理的桥梁——Agent 执行行动后，必须感知环境反馈，才能做出合理的下一步决策。

## 全景图：Agent 技术的五层架构

我把 Agent 技术栈拆分为五个层次，自底向上分别是：

```
第1层：基础设施       →  向量数据库 / API网关 / 监控可观测
第2层：LLM 能力层     →  模型选型 / 微调（SFT/GRPO） / 评测
第3层：核心能力模块    →  决策推理 / 记忆管理 / 工具调用
第4层：Agent框架与运行时 →  LangGraph / LangChain / 自研框架
第5层：多Agent协作与编排 →  A2A架构 / 任务分解 / 协议设计
```

### 第1层：基础设施

这是最容易忽视、但又最影响系统稳定性的部分。

**核心组件：**
- **向量数据库**：用于语义检索和记忆存储（Milvus、Pinecone、Chroma）
- **API 网关与限流**：LLM 调用的统一入口，负责负载均衡、熔断、计费
- **监控与可观测**：Agent 调用链路追踪、Token 消耗统计、异常告警
- **缓存层**：相似问题缓存、Embedding 缓存，降低重复调用成本

**实用建议：** 很多团队在搭建 Agent 系统时直接跳到框架选型，忽略了基础设施的规划。结果上线后发现成本不可控、调用链路无法追踪、问题难以排查。先把第1层打好，后面会省很多事。

### 第2层：LLM 能力层

Agent 的智能核心，直接决定了上层的决策质量。

**核心议题：**
- **模型选型**：GPT-4o、Claude、Qwen、GLM 等模型在 Agent 场景的适用性对比
- **微调方法**：SFT（监督微调）用于格式化和领域知识注入，GRPO（Group Relative Policy Optimization）用于强化推理能力
- **模型评测**：如何量化评估模型在 Agent 场景的表现，而不只是看 benchmark 分数
- **成本与性能的平衡**：不同子任务使用不同等级的模型，用 MoE 思路做模型路由

**关键洞察：** 做垂域 Agent 时，通用大模型往往是"通而不精"。通过 SFT 注入领域知识，再通过 GRPO 强化推理链条的质量，可以显著提升 Agent 在特定场景下的表现。但这不是万能药——微调有成本，且会牺牲一定的通用性，需要根据实际场景做 trade-off。

### 第3层：核心能力模块

这是 Agent 技术的"心脏"，包含了三个最核心的模块。

**决策推理模块：**
- **ReAct**（Reasoning + Acting）：最经典的决策模式——先推理出下一步做什么，再执行，然后观察结果，循环往复
- **Reflection**：在决策链条中加入自我反思环节——"我刚才那步做得对吗？需不需要调整策略？"
- **Reflexion**：更高级的自我改进——把失败经验存入记忆，避免在后续任务中重复犯错
- **Plan-and-Execute**：先制定完整计划，再逐步执行，适合复杂多步骤任务

**记忆管理模块：**
- **短期记忆**：当前对话上下文窗口的管理（截断、摘要、滑动窗口）
- **长期记忆**：跨会话的知识积累（向量存储、知识图谱、结构化数据库）
- **工作记忆**：任务执行过程中的中间状态维护（思维链记录、子目标追踪）

**工具调用模块：**
- **函数调用（Function Calling）**：模型原生支持的工具调用能力
- **工具描述优化**：如何写好工具的 prompt，让模型准确选择和调用
- **工具组合与编排**：多个工具之间的依赖关系和调用顺序
- **错误处理与重试**：工具调用失败时的优雅降级策略

**实用建议：** 三个模块不是独立存在的。一个好的 Agent 系统，记忆管理为推理提供上下文，推理决策为工具调用提供依据，工具执行的结果又写回记忆——三者形成一个紧密的闭环。

### 第4层：Agent 框架与运行时

当你需要把上面的能力模块"组装"成一个可运行的系统时，框架就派上用场了。

**主流框架对比：**

| 维度 | LangChain | LangGraph | 自研框架 |
|------|-----------|-----------|----------|
| 定位 | 通用 LLM 应用框架 | 图结构状态机框架 | 完全定制 |
| 学习曲线 | 中等 | 较陡 | 取决于复杂度 |
| 灵活性 | 高 | 很高 | 最高 |
| 生产就绪度 | 成熟 | 快速成熟中 | 需自建 |
| 适用场景 | 快速原型、简单链式调用 | 复杂状态机、条件分支 | 特殊需求、性能要求极高 |

**选择建议：**
- 如果是快速验证想法，LangChain 足够用
- 如果业务逻辑复杂，需要状态管理和条件分支，LangGraph 是更好的选择
- 如果有特殊的性能需求、安全要求，或者现有的框架都不满足，再考虑自研
- 不要为了自研而自研，框架只是工具，解决业务问题才是目的

### 第5层：多 Agent 协作与编排

这是 Agent 技术的前沿方向，也是目前我在工作中花精力最多的地方。

**核心概念：**
- **Agent-to-Agent（A2A）架构**：多个专业化的 Agent 通过协议协作完成复杂任务
- **任务分解**：将复杂目标拆解为子任务，分配给不同能力的 Agent
- **通信协议**：Agent 之间的消息格式、交互流程、错误处理
- **编排策略**：中心化编排（一个调度 Agent 统一管理）vs 去中心化协作（Agent 之间直接通信）

**典型场景：**
- 一个物流调度任务，需要"路线规划 Agent"+"车辆匹配 Agent"+"实时监控 Agent"协同工作
- 一个代码开发任务，需要"需求分析 Agent"+"代码生成 Agent"+"测试 Agent"流水线协作
- 一个客户服务场景，需要"意图识别 Agent"+"知识检索 Agent"+"工单创建 Agent"按需联动

**关键挑战：**
- 如何定义 Agent 之间的责任边界，避免互相推诿或重复劳动
- 如何设计通信协议，保证信息传递的准确性和效率
- 如何处理 Agent 执行失败的情况，保证系统的容错性
- 如何评估多 Agent 系统的整体表现，而不只是单个 Agent 的表现

## 学习路径建议

如果你刚开始接触 Agent 技术，我建议按以下顺序学习：

**第1阶段（入门）：理解概念**
- 读这篇全景图 + 了解 ReAct 基本原理
- 用 LangChain 写一个最简单的工具调用 Agent
- 目标：能说清楚 Agent 和普通 LLM 调用的区别

**第2阶段（进阶）：核心模块实践**
- 深入 ReAct / Reflection / Plan-and-Execute 的实现细节
- 搭建 Memory 模块，理解短期/长期/工作记忆的不同实现方式
- 学习 Function Calling 的 prompt 工程技巧
- 目标：能独立搭建一个单 Agent 系统，完成有明确步骤的任务

**第3阶段（实战）：框架选型与工程化**
- 用 LangGraph 搭建有状态管理的 Agent
- 学习 Agent 评测方法，建立自己的评测体系
- 实践垂域模型微调（SFT + GRPO）
- 目标：能交付一个生产可用的 Agent 系统

**第4阶段（前沿）：多 Agent 协作**
- 理解 A2A 架构的设计模式
- 实践任务分解与 Agent 编排
- 学习多 Agent 系统的评测与优化
- 目标：能设计并实现复杂的多 Agent 协作系统

## 后续文章预告

这张全景图就是本公众号的内容地图。后续文章会按图索骥，逐一展开每一层的核心内容：

- **下一篇**：《ReAct 深度解析》——从原理到实现，手把手带你写一个自己的 ReAct Agent
- **之后**：Memory 模块设计、LangGraph 实战、Agent 评测体系建设、多 Agent 协作协议设计...

每篇文章我都会在开头标注它在全景图中的位置，方便你建立系统化的认知。

---

*如果你觉得这篇文章有帮助，欢迎转发给可能需要的朋友。有任何问题或想聊的话题，随时在后台留言。*
